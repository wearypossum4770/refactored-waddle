pub fn almost_increasing_sequence(sequence){

}
#[cfg(test)]
mod tests {
    use super::almost_increasing_sequence;
    #[test]
    fn test_almost_increasing_sequence() {
        assert_eq!(addition(2, 3), 5);
        assert_eq!(addition(-3, -6), -9);
        assert_eq!(addition(7, 3), 10);
        assert_eq!(addition(88, 2), 90);
    }
}